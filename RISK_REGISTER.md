# Risk Register

The current system has no account access, positions, orders, or execution path. These risks concern
Phase 2 public market data and engineering control state. Policy is defined in
`docs/RISK_POLICY.md`; material changes require an ADR and the approvals in `docs/POLICIES.md`.

| ID | Risk | State | Current controls | Next treatment |
|---|---|---|---|---|
| RISK-001 | Public sources can be unavailable, slow, incomplete, reordered, or semantically inconsistent. | Open / monitored | Bounded requests, classified retries, settlement delay, quality gates, explicit gaps, source-health evidence, and fail-closed admission. Finite-positive timeouts are validated at the shared transport and every active provider boundary before request or provider work; they retain standard transport semantics and are not claimed as total wall-clock deadlines. Supported transport failures raised while reading either a successful body or an HTTP-error body become one sanitized typed failure with the read error as the direct cause, without a retry or partial response. Body-read and direct pre-response `IncompleteRead` failures follow the same boundary without accepting or returning their partial bytes or expected-byte count or including those details in the public message; response entry/exit behavior is unchanged. `BadStatusLine`, `LineTooLong`, and `UnknownProtocol` raised during direct acquisition or either bounded body read now follow the same sanitized direct-cause boundary; direct base `HTTPException`, `InvalidURL`, response entry/exit, and protocol cleanup-only behavior are unchanged. Every HTTP-error response path attempts cleanup exactly once, successful cleanup closes before return or propagation, and no cleanup failure can replace an existing primary outcome. An `OSError`-family or `IncompleteRead` cleanup-only failure is typed and sanitized; a failed cleanup attempt is not treated as proof of closure. Automatic 301, 302, 303, 307, and 308 redirects are rejected by one private no-follow urllib opener before urllib inspects or parses any `Location` or `URI` redirect-target value, drains the redirect body inside the handler, or issues a second-destination request. The original 3xx is materialized through the existing bounded `HTTPError` path with one sentinel read and one cleanup attempt; missing, empty, relative, same-origin, cross-origin, downgrade, FTP, unsupported-scheme, malformed, and `URI`-header targets are not followed, and the process-global opener is untouched. After timeout validation and before query, request, or opener work, the original initial target must be an absolute credential-free HTTPS URL with a non-empty CPython-parser hostname, no literal query or fragment delimiter, backslash, whitespace, C0/DEL, lone surrogate, userinfo, ambiguous percent-encoded authority, or invalid explicit port. Parser failures and later-IDNA NFKC authority ambiguity fail with one exact context-suppressed structural error; an accepted target is never reconstructed or normalized. After that structural boundary and still before query access, the caller target port must be omitted or parse as numeric 443. A structurally valid nonstandard target port fails with one exact cause-free error and no downstream work; accepted implicit, explicit, and zero-padded 443 text remains unchanged. This caller-authority control does not constrain a configured proxy peer. It performs no hostname allowlisting, DNS resolution, IP/public-routability check, or SSRF guarantee and still accepts every parser-permitted host form. Query serialization now takes one adapter-bounded snapshot before encoding: no more than 32 exact built-in-string pairs, 8,192 cumulative key-plus-value characters, or 33 yielded items are consumed. Invalid shape, type, count, or volume fails exactly before encoding or request work, while mapping-originated failures remain raw and accepted pairs retain their existing sorted encoding. After timeout validation and before content-dependent URL work, the original target's non-polymorphic built-in string length is capped at 8,192 Python characters. Oversize fails with one exact cause-free error before scanning, parsing, query, or request work; exact-limit ASCII and multi-byte Unicode text remains unchanged, and caller length/content overrides are never dispatched. This is not an encoded-byte, request-line compatibility, total-wall-clock, hostname, DNS, IP-routability, or SSRF guarantee. The configured User-Agent is now an exact built-in string of 1 through 256 visible-ASCII characters, validated during client construction without normalization, fallback, or privacy claims. Finite-positive public-HTTP timeouts now also share a 120-second maximum at the transport and every active provider constructor, without changing their standard per-operation semantics, accepted-object identity, retries, waits, pacing, or rate budgets. After the bounded body-size decision, successful and HTTP-error response headers now use one adapter-bounded snapshot. It calls `headers.items()` and starts its iterator once, pulls at most 101 times, accepts at most 100 pairs and 65,536 cumulative name-plus-value Python characters, and fails a yielded 101st pair before unpacking or inspection. Accepted order, duplicates, casing, and content remain exact; body-first and cleanup precedence and raw header-origin failures are preserved. Standard-library parsing and prior allocations occur before the projection, so it makes no wire-header, parser-allocation, total-memory, total-wall-clock, privacy, redaction, content-policy, hostname, DNS, IP-routability, or SSRF guarantee. TASK-056 now adds one deterministic generated-fixture drill in which an exhausted retryable disconnect retains the exact pending leaf at failed version 3 and a process-style reopen with newly constructed evidence, control, and rate-budget adapters plus a newly constructed orchestrator completes sparse windows at version 6 under a fresh fence. Five durable reservations precede five requests, all transition and health evidence remains causal, and a completed rerun performs no work. ADR-0028 records only a conceptual finite-run single-host composition with closed UTC windows, bounded catch-up, clean stop, separate source/service health, failure and rollback rules, and no implemented runtime or readiness claim. TASK-059 now adds only a pure unused fixed-UTC closed-window planner and strict lifecycle values whose results are `HELD`, `WAITING`, or `ATTACHED_JOB`; they perform no action and create no runtime path. ADR-0029 now defines only the conceptual stream persistence boundary: exact-identity load and versioned compare-and-swap fail closed; attachment requires complete canonical child-creation bytes because a fingerprint alone is not reconstructable; and every crash seam resolves through exact reload without invented progress. It adds no store, network, retry, recovery, or automatic action. TASK-061 now implements only an unused persistence-record and canonical-codec contract: strict versioned child, envelope, create, transition, and evidence-scope models; five canonical JSON codecs; six domain-separated digest/history contracts; a two-pass finalizer; exact load, scope, and link validators; and hostile deterministic tests. It adds no port, store, adapter, schema, I/O, runtime, authority, action, or readiness. TASK-062 now freezes only an unused logical store port around exact finalized TASK-061 bytes and records, closed outcomes, one-winner compare-and-swap ownership, and bounded audit pages. It performs no provider work, retry, physical storage, or runtime action. | TASK-063 is now the canonical next design-only physical stream-store architecture and evidence-plan task; it authorizes no adapter, database, implementation, capacity, durability, or readiness claim. Preserve the TASK-056 disconnect and sparse-recovery evidence; continuous-operation readiness, automatic recovery, cross-database atomicity, physical durability, and operational capacity remain unproven. |
| RISK-002 | A provider can change payload shape, limits, precision, ordering, or endpoint semantics without notice. | Open / monitored | Strict adapter validation, provider-independent canonical contracts, raw-byte lineage, response-cap checks, and contract tests. Every active provider JSON boundary converts invalid UTF-8, malformed JSON, excessive nesting, and decoder `ValueError` into a sanitized, non-retryable typed provider error. The shared public transport accepts only an exact built-in integer response-body cap from 1 through 2,000,000 bytes and uses a one-byte overflow sentinel for both successful and HTTP-error bodies; oversized bodies fail without returning truncated evidence. TASK-057 adds one strict exact-byte, SHA-256-pinned version-1 manifest and five bounded synthetic fixtures for the five active public-provider request variants. Offline deterministic stubs pass every exact fixture through its existing production adapter. Binance candle rows retain 12 positions, Coinbase rows six, and both aggregate variants retain the shared exact required set `T`, `a`, `f`, `l`, `m`, `p`, `q` and optional set `M`, `nq`; Spot fixture presence of `M` and USD-M fixture presence of `nq` do not create market-specific parser rules. Representative detectable width, selected detectable reorder, type, invalid-decimal, missing-required, invalid-present-optional, and unknown-field cases fail closed. Decimal precision alone and some same-typed semantic reorder may still parse, so acceptance is not compatibility proof and either unreviewed change requires the manual pause/review/resume procedure. The runbook adds no automatic detector, payload store, pause, remediation, resume, or continuous-operation readiness. ADR-0028 preserves that exact manual hold and governed resume boundary in its conceptual future composition and grants no detector or automatic action. TASK-059 models a validated paused checkpoint as a pure input whose planner outcome is only `HELD`; it does not inspect payloads, detect drift, write a hold, retry, remediate, or resume. ADR-0029 requires explicit actor/governance evidence for a future hold or resume, preserves cursor and attachment across both, and rejects missing, stale, conflicting, rejected, or revise-required authority. A pause reason alone is not authorization. TASK-061's strict versioned evidence-scope records, canonical codec, and exact scope/link validators now preserve the governed hold/resume evidence references as unused canonical data and fail hostile deterministic cases closed; they neither inspect provider payloads nor detect drift, grant authority, or perform an action. TASK-062 adds only strict finalized-record storage commands/results and bounded store-local structural views; it does not inspect provider payloads, evidence bodies, or authority. | TASK-063 is now the canonical next design-only physical-store architecture and evidence-plan task; it authorizes no adapter, database, implementation, capacity, durability, or readiness claim. Preserve the TASK-057 fixtures, parser limits, and manual runbook; implementation and operating readiness remain separately governed. |
| RISK-003 | Cooperating collectors can exceed provider rate limits or local work bounds, especially across crashes or future hosts. | Open / controlled locally | The bounded orchestrator retains shared durable weighted budget gating before provider access; ranges, requests, records, splits, retries, and waits remain finite. ADR-0028 requires one shared durable single-host budget before every future provider request, finite catch-up and invocation work, and explicit cooperative shutdown-check boundaries, but implements and reserves no capacity. TASK-059 validates only finite pure policy and planner bounds and does not reserve, spend, or persist capacity. ADR-0029 keeps the full budget and every reservation in its existing separate durable domain; stream attachment and compare-and-swap grant no capacity and cannot bypass the shared pre-request gate. Because TASK-059's stream-policy fingerprint is caller-supplied rather than derived, ADR-0029 separately binds the complete immutable stream-policy projection in creation evidence and requires field-for-field agreement as well as fingerprint equality. TASK-061 now binds versioned policy identity and the exact complete stream-policy projection through strict unused records, canonical codecs, domain-separated digests, and exact load/scope/link validation. It implements no budget accounting, reservation, provider gate, authority, action, or runtime. TASK-062's logical store results and retry dispositions grant no request budget, provider permission, capacity, or automatic retry. | TASK-063 is now the canonical next design-only physical-storage architecture and evidence-plan task, including capacity prerequisites; it authorizes no adapter, database, implementation, capacity grant, durability, or readiness claim. Preserve pre-request gating in any later continuous collector; do not claim capacity adequacy, multi-host safety, or crash-durable per-job hard limits without separate measured operational evidence and reservation design. |
| RISK-004 | Market evidence and checkpoint control state can diverge because their SQLite databases cannot commit atomically. | Open / recovery tested and inspectable locally | Evidence-first orchestration, idempotent market storage, exact pending-leaf recovery, typed terminal mapping, compare-and-swap transitions, UUID fencing, both crash-seam tests, and bounded typed causal inspection of every retained actor transition. Candle and order-flow admission now bind returned raw identity and exact ordered, status-coherent write outcomes to every fetched batch record; order-flow admission also binds each record family. Hostile-store evidence proves malformed outcomes cannot advance either checkpoint flow even when physical first-page or first-window writes exist. TASK-056 adds one composed generated-fixture drill through newly constructed evidence, checkpoint, and rate-budget SQLite adapters: worker A fails at version 3 with the exact pending leaf, worker B completes at version 6 under a fresh fence, all six transitions and matching health versions remain causal, and a completed rerun changes nothing. Returned outcomes and generated fixtures still cannot independently attest physical durability or make the market and checkpoint databases atomic. ADR-0028 requires an attached child to retain its exact pending leaf and complete after accepted evidence before a future continuous cursor advances; that ordering is conceptual only. TASK-059 adds only pure attachment and transition validation with no database or cursor write. ADR-0029 now fixes the logical ordering for attachment, child creation, request/budget, market evidence, exact pending-leaf progress, child completion, stream advancement, lifecycle evidence, hold, and resume. An unknown commit is resolved only by exact reload; a completed attached child may advance with zero provider requests. Stream, child, evidence, lifecycle, and budget stores remain explicitly non-atomic, and compare-and-swap is not a fresh UUID fence. Six separate digest contracts bind child creation, current envelopes, creation/transition records, typed evidence scope, and a rolling history root. Governed create is the sole bootstrap; a bounded creation audit must attest version one before any post-create action. Current load/planning grants no action: an externally anchored attestation matching the exact version, envelope digest, and cumulative root is a prerequisite for future child create/recovery, claim, budget reservation, provider request, evidence admission, or later stream mutation. TASK-061 now implements the pure unused logical evidence contract for those seams: strict versioned child, envelope, create, transition, and evidence-scope models; five canonical JSON codecs; six domain-separated digest/history contracts; a two-pass finalizer; exact load, scope, and link validators; and hostile deterministic tests. It creates no port, store, adapter, schema, I/O, attestation service, runtime, authority, action, physical-durability evidence, or readiness claim. TASK-062 now defines only logical atomic create/CAS ownership, exact retained-byte wrappers, and bounded current/audit outcomes; no transaction or physical durability exists yet. | TASK-063 is now the canonical next design-only physical-store architecture and preimplementation evidence-plan task; it authorizes no adapter, database, implementation, capacity, durability, or readiness claim. Preserve the composed recovery drill when any later collection path changes, require separately governed crash-seam and operational evidence before continuous collection, and keep cross-database atomicity and physical durability as explicit residual risks. |
| RISK-005 | Remaining timestamp contracts and persisted JSON or text may preserve a caller offset, including newer stores whose indexed projections are UTC. Mixed textual offsets can also sort incorrectly. | Open / additive canonical evidence foundations incomplete | Every scoped injected clock now fails unless `tzinfo is datetime.UTC`, before the next ID, HTTP, storage, reservation, wait, log, or canonical-evidence side effect. Pure unused helpers provide strict fixed-UTC validation, explicit aware-input normalization, exact six-digit RFC 3339 `Z` serialization/parsing, and exact integer-only epoch-microsecond projection/decoding across Python's full calendar range. TASK-030 pins all eight generated SQLite layouts; TASK-031 adds same-connection, exact-snapshot, bounded raw evidence for all 37 direct timestamp columns; TASK-032 purely interprets that evidence through the reviewed writer/storage declarations; TASK-033 retains every ordered outcome while deriving exact fixed-UTC, canonical `Z`, and epoch-microsecond candidate triples only for successful parses; TASK-034 adds exact family-scoped censuses of totals, exhaustive statuses, source offsets and precision, and projectable epoch extrema for all 20 tables and 37 columns; TASK-035 retains the exact eight-family sequence while reconciling only those existing counts, frequencies, and projectable extrema into one strict pure bundle; and TASK-036 adds only a private-TASK-035-plan-pinned synthetic proposal with eight exact non-path family slots, unselected boundary placeholders, and fixed no-authority states. Those slots prove synthetic coverage only and do not assert future real path cardinality. Strict writer round trips, signed-64-bit validation, deep evidence linkage, typed prior failures and calendar overflow, inverse candidate agreement, separate retention of equal instants, exact empty and mixed-column reconciliation, pre-aggregation source validation, and deep proposal validation preserve every source byte and authority boundary. Provider/application mappings and legacy request/model acceptance remain unchanged. No existing model, serializer, query, index, projection, permission, or persistence runtime consumes these foundations; no operator database has been scanned, no replacement timestamp has been selected, and most historical bytes remain aware-only. The [TASK-026 inventory and plan](docs/CANONICAL_UTC_BOUNDARY_INVENTORY_AND_MIGRATION_PLAN.md) remains the migration authority. ADR-0029 keeps TASK-059 epoch-millisecond cursor values exact and explicitly records that their full accepted range cannot always be converted to Python datetimes or signed-64-bit epoch microseconds. A future store must preserve them or separately tighten the contract and must never truncate, wrap, or silently normalize them. | TASK-037 is a RISK-3 authorization decision limited to preparing the exact operator-preflight package and obtaining an explicit project-owner `APPROVE`, `REJECT`, or `REVISE` outcome with independent Risk and Security review. It may record proposed real path-list metadata and cardinality, the writer-fenced consistent/immutable snapshot procedure, report destination, retention/disposal boundary, monitoring, rollback, and policy-required decision metadata only through approved governance handling. It may not inspect, resolve, check, or open a proposed operator path or database; access SQLite or operator data; scan rows; invoke an adapter; create an operational report or manifest; add a serializer or scanner; wire a runtime; migrate; alter a schema; or perform Stage 3. Approved governance-artifact writes are the only filesystem mutation in scope. Missing, ambiguous, expired, conflicting, rejected, or revise-required authority remains denial; any scanner remains a separately scoped later task. Compatibility readers, version-2 schemas, quarantine, backup/rollback, and migration verification remain required before global closure. |

RISK-004's current-attestation gate has one ADR-0028-compatible exception only: a provider
operation already past every pre-request gate under the exact accepted pre-hold attestation, fence,
and authority may admit its already-returning evidence and finish or fail that same finite child
after a concurrent manual hold only when validated hold evidence explicitly classifies a
non-integrity operational hold and preserves the response/admission contract. Drift, invalid
payload, quality/evidence failure, corruption, or ambiguous classification stops canonical
admission/progress and permits only a separately governed quarantine/attention path. It starts no
new attempt/request and performs no stream mutation; all later work again requires an attestation
matching current stream state and every applicable fresh gate. ATTACH authority itself is
time-independent: it binds exact prior version, envelope digest, accepted history root, successor
version, candidate child UUID, and effective child-policy fingerprint. The trusted finalized
transition and rolling root bind the resulting successor and creation fingerprint after the single
clock sample.

## TASK-063 Physical Store Design Treatment

ADR-0031 adds only a design-level treatment and changes no risk state:

- **RISK-001:** a dedicated local SQLite design preserves exact TASK-061 bytes, immutable
  identity/policy bindings, closed failures, and bounded audits; it adds no provider path,
  automatic retry, recovery, durability, capacity, or continuous-operation claim.
- **RISK-002:** canonical records and exact byte projections do not inspect provider payloads,
  detect schema drift, or grant hold/resume authority. TASK-057 fixtures, parser limits, and the
  manual pause/review/resume boundary remain required.
- **RISK-003:** host quota, `max_page_count`, WAL/checkpoint bounds, maximum reader duration,
  workload, latency, and storage reserve remain mandatory unproved finite gates. A page size or
  theoretical SQLite limit is not capacity evidence, and the stream store grants no request budget.
- **RISK-004:** the proposed transaction and deferred witness/tail bindings can make only the
  dedicated stream database's current row and immutable history row atomic. Market, child,
  evidence, lifecycle, health, fence, and budget stores remain separate and non-atomic; crash,
  backup, restore, target-filesystem, and operational evidence remain prerequisites.
- **RISK-005:** every TASK-059 epoch millisecond remains exact over `0` through `2**63 - 1`. The
  SQL-projected stream-start epoch and causal versions use signed-64-bit SQLite `INTEGER`; current
  cursor and optional attachment-window epochs deliberately have no scalar columns and remain
  exact inside authoritative TASK-061 BLOBs. No datetime, floating-point, epoch-microsecond,
  truncating, wrapping, or clamping projection is permitted. No schema, adapter, database,
  migration, or operator-data access is executed.

TASK-063 remains the canonical design-only next action until its governed lifecycle completes. It
authorizes no adapter, database, implementation, capacity, durability, recovery, readiness, or
operating-mode change. TASK-037 remains blocked and authorization remains denied.

## Escalation

- A new critical risk, suspected credential exposure, corrupt trusted evidence, or bypassed safety
  control stops the affected workflow and is recorded immediately.
- High or critical residual risk cannot be silently accepted by code, CI, an agent, or a task
  author. It requires explicit project-owner approval and a documented expiry or treatment.
- Resuming after a safety halt requires the human approval specified in `docs/POLICIES.md`.

Review this register whenever a provider contract, storage schema, permission, operating mode,
execution boundary, or canonical source of truth changes.
